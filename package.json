{
  "name": "layout-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && node index.js",
    "dev": "echo 'Run server and client separately — see README.md'"
  }
}
